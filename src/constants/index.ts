export const nameBrand: string = 'Hallivis ®';

const date = new Date();

export const getYear = date.getFullYear();

export const FLICKITY_OPTIONS = {
    initialIndex: 1,
    pageDots: false,
    wrapAround: true,
    contain: true,
    freeScroll: true,
    accessibility: false,
};